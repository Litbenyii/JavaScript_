// Crea un array de números y usa reduce para obtener la suma total.

let numeros = [0, 7, 5, 3, 1];

let sumaT = numeros.reduce((acumulador, numero) => acumulador + numero);

console.log(sumaT);
